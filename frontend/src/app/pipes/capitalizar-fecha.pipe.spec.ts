import { CapitalizarFechaPipe } from './capitalizar-fecha.pipe';

describe('CapitalizarFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizarFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
